EXPO_PUBLIC_SUPABASE_URL=https://xfphsdwdsaptunvgjxfk.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_C0l2rPBZnkRSibtNUpynLQ_OYktcCy6

# Optional helper for local CLI tasks like `npm run supabase:push:remote`.
# Set this in your local shell or uncommitted `.env` file before using DB tooling.
# Leave it blank in the committed example so the repo never carries a real or templated DB URI.
SUPABASE_DB_URL=

# Optional local cache override for USDA release archives and extracted JSON.
# USDA_DOWNLOAD_DIR=tmp/usda

# The USDA importer uses official USDA download files and does not require an API key.
